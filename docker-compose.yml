version: "3.9"
services:
  fishtrade:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ADMIN_TOKEN=${ADMIN_TOKEN:-devkey}
      - PORT=8080
    volumes:
      - ./data:/app/data
    restart: unless-stopped
